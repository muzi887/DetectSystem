本项目代码量统计如下（排除 `node_modules`、`dist`、`.git`、`docs`）。

## 总览

| 口径 | 文件数 | 总行数 | 非空行（有效代码） |
|------|--------|--------|-------------------|
| **全项目代码** | 51 | 7,996 | **7,042** |
| **生产代码**（`src/` + `server/` + `deploy/api_mock/`） | 44 | 7,437 | **6,538** |
| **前端 `src/`** | 41 | 6,866 | **6,146** |

> 软著/export 常用「非空行」口径；本项目生产代码约 **6,500+ 行**，满足一般软著体量要求（通常 3,000 行以上即可）。

---

## 按语言

| 类型 | 文件数 | 总行数 | 非空行 |
|------|--------|--------|--------|
| Vue（`.vue`） | 16 | 5,192 | 4,592 |
| TypeScript（`.ts`） | 24 | 1,550 | 1,362 |
| JavaScript（`.js/.cjs/.mjs`） | 6 | 564 | 500 |
| Python（`.py`） | 2 | 391 | 326 |
| 样式（`.css`） | 3 | 309 | 262 |

Vue 占主体（约 **65%** 有效代码），符合以页面业务为主的 Web 项目特征。

---

## 按目录（生产代码）

| 目录 | 文件数 | 非空行 | 说明 |
|------|--------|--------|------|
| `src/views/` | 8 | 3,498 | 业务页面（最大头） |
| `src/layouts/` | 1 | 625 | 顶栏、导航、搜索 |
| `src/components/` | 6 | 465 | 遥感地图、空状态等 |
| `src/stores/` | 3 | 399 | Pinia 状态 |
| `src/composables/` | 4 | 346 | 地图、检索等组合逻辑 |
| `src/utils/` | 4 | 227 | 状态机、预警级别等 |
| `src/router/` | 4 | 102 | 路由与权限 |
| `src/mock/` | 1 | 87 | Mock 路由注册 |
| `server/` | 1 | 120 | Flask 图像分析 |
| `deploy/api_mock/` | 2 | 272 | 农业业务规则 `agriMockCore` |

---

## 最大源文件 Top 5（非空行）

| 非空行 | 文件 |
|--------|------|
| 883 | `src/views/user/RelatedData.vue` |
| 625 | `src/layouts/AppLayout.vue` |
| 586 | `src/views/user/About.vue` |
| 501 | `src/views/user/DataAnalysis.vue` |
| 408 | `src/views/user/DecisionSupport.vue` |

8 个业务页面合计约 **3,500 行**有效代码。

---

## 未计入生产代码的部分

| 类别 | 非空行 | 说明 |
|------|--------|------|
| `scripts/` | 262 | 导出脚本、资源优化等工具 |
| 根目录配置 | 227 | `vite.config.ts`、`eslint.config.js` 等 |
| Mock 数据 `db.json` | ~1,100 | 演示数据，非逻辑代码 |

---

**一句话**：本项目有效生产代码约 **6,500 行**，前端 Vue 页面与布局占约 **70%**，领域逻辑集中在 `monitorStatus.ts`、`agriMockCore.cjs`、`remoteSensing` 等模块。若软著附录需凑够前 30 + 后 30 页（约 3,000 行有效代码），当前代码量已足够，优先放 `RelatedData.vue`、`AppLayout.vue` 及附录 D 中的业务模块即可。