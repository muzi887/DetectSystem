# 2.0 非 AI · P0 天气预报 UI Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 相关数据「气象」Tab 展示选中站未来 7 日气温/降水，极端天气标签可点进预警中心。链 2 已能扫预报写 `[极端天气]`，本计划只补页面，不改规则。

**Architecture:** 继续用扁平表 `weatherForecast`（一行一天，`pointId + date`）和已有 `GET /weatherForecast`、`GET /extremeEvents`（`src/api/rules.ts` 的 `fetchForecast` / `fetchExtremeEvents`）。前端按 `selectedWeatherPointId` 过滤。不新建 `weather.ts`，不接和风。

**Tech Stack:** Vue 3、Ant Design Vue、json-server。测逻辑用 `pnpm exec tsx --test`；页面用 `pnpm exec vue-tsc --noEmit`。

**Spec:** [`2.0非AI模块实现方案.md`](../方案/2.0非AI模块实现方案.md) §4.2 未完成项。前置：[`规则链-2.0总线实施计划.md`](./规则链-2.0总线实施计划.md) Task 1–7 已完成。

## Global Constraints

- **禁止**改 Flask、P3、`evaluateDisasterRules` 落库；**禁止**重写链 2。
- 预报行必须能被现有 `evaluateForecast` 读：字段 `date`、`tempMax`、`tempMin`、`precipMm`、`windMax`，可选 `humidity`。
- 雄县 `pointId: 2` 至少保留 1 日 `tempMax >= 40`，保证链 2 仍能命中极端高温。
- 不新增 vitest；不上 WebSocket。
- 改了 `src/mock/db.json` 后跑 `pnpm run sync:mock-db`。

### File map

| File | Responsibility |
|------|----------------|
| `src/utils/forecastView.ts` | 按站过滤、按日期排序、最多取 7 天 |
| `src/utils/forecastView.test.ts` | 过滤与排序 |
| `src/mock/db.json` | 雄县补满 7 日；河间给 7 日普通预报 |
| `src/api/rules.ts` | `fetchForecast` 已有，可加 `?pointId=` |
| `src/views/user/RelatedData.vue` | 7 日表 + 极端标签跳转 `/warnings` |

---

### Task 1: 按站取 7 日预报纯函数 + 种子

**Files:**
- Create: `src/utils/forecastView.ts`
- Test: `src/utils/forecastView.test.ts`
- Modify: `src/mock/db.json` 的 `weatherForecast`

**Interfaces:**
- Consumes: 扁平行 `{ pointId, date, tempMax, tempMin, precipMm, windMax, humidity? }`
- Produces: `daysForPoint(rows, pointId, limit = 7)` → 按 `date` 升序、最多 `limit` 条

- [ ] **Step 1: Write the failing tests**

Create `src/utils/forecastView.test.ts`:

```ts
import assert from 'node:assert/strict'
import { test } from 'node:test'
import { daysForPoint } from './forecastView.ts'

const rows = [
  { pointId: 2, date: '2026-08-24', tempMax: 34, tempMin: 22, precipMm: 2, windMax: 5 },
  { pointId: 1, date: '2026-08-22', tempMax: 30, tempMin: 20, precipMm: 0, windMax: 3 },
  { pointId: 2, date: '2026-08-22', tempMax: 41, tempMin: 24, precipMm: 0, windMax: 3 },
  { pointId: 2, date: '2026-08-23', tempMax: 36, tempMin: 23, precipMm: 0, windMax: 4 }
]

test('filters by point and sorts by date', () => {
  const days = daysForPoint(rows, 2, 7)
  assert.equal(days.length, 3)
  assert.equal(days[0].date, '2026-08-22')
  assert.equal(days[0].tempMax, 41)
  assert.equal(days[2].date, '2026-08-24')
})

test('caps at limit', () => {
  assert.equal(daysForPoint(rows, 2, 1).length, 1)
})
```

- [ ] **Step 2: Run test to verify it fails**

Run: `pnpm exec tsx --test src/utils/forecastView.test.ts`

Expected: FAIL `Cannot find module` 或 `daysForPoint is not a function`

- [ ] **Step 3: Write minimal implementation**

Create `src/utils/forecastView.ts`:

```ts
export type ForecastRow = {
  pointId: number
  date: string
  tempMax: number
  tempMin: number
  precipMm: number
  windMax: number
  humidity?: number
}

export function daysForPoint(
  rows: ForecastRow[],
  pointId: number,
  limit = 7
): ForecastRow[] {
  return [...rows]
    .filter((row) => Number(row.pointId) === Number(pointId))
    .sort((a, b) => a.date.localeCompare(b.date))
    .slice(0, limit)
}
```

- [ ] **Step 4: Run tests**

Run: `pnpm exec tsx --test src/utils/forecastView.test.ts`

Expected: PASS

- [ ] **Step 5: 把 `weatherForecast` 补到每站 7 日**

`pointId: 2`（雄县）日期 `2026-08-22`～`2026-08-28`，其中 22 日 `tempMax: 41` 保留。其余日最高温 30–36、降水可有 0～8 mm。

`pointId: 1`（河间）同样 7 日，最高温不超过 35（避免演示时两个站都刷极端高温）。其它监测点可以没有预报，切到该站时表格显示「暂无 7 日预报」。

现有 `id: 1..3` 可改日期/补行，新行 `id` 从 4 递增。不要改成嵌套 `days: []`（链 2 读的是扁平行）。

- [ ] **Step 6: 同步部署包**

Run: `pnpm run sync:mock-db`

---

### Task 2: 气象 Tab 7 日表 + 标签跳转预警

**Files:**
- Modify: `src/api/rules.ts`（可选：`fetchForecast(pointId?: number)`）
- Modify: `src/views/user/RelatedData.vue`

**Interfaces:**
- Consumes: `daysForPoint`、`fetchForecast`、`fetchExtremeEvents`（已有）
- Produces: 气象 Tab 在快照卡片下方出现 7 日表；橙色极端标签可进 `/warnings`

- [ ] **Step 1: API 按站拉预报**

`src/api/rules.ts` 改为：

```ts
export const fetchForecast = (pointId?: number) =>
  http.get('/weatherForecast', { params: pointId ? { pointId } : undefined })
```

json-server 支持 query `pointId`。不要新建 `/weather/forecast`。

- [ ] **Step 2: RelatedData 加载与展示**

在现有 `extremeEvents` 旁增加：

```ts
import { useRouter } from 'vue-router'
import { daysForPoint, type ForecastRow } from '@/utils/forecastView'
import { fetchExtremeEvents, fetchForecast, fetchThresholds, saveThresholds } from '@/api/rules'

const router = useRouter()
const forecastDays = ref<ForecastRow[]>([])

async function loadForecast(pointId: number) {
  try {
    const res = await fetchForecast(pointId)
    forecastDays.value = daysForPoint(res.data || [], pointId, 7)
  } catch {
    forecastDays.value = []
  }
}
```

`onMounted` 的 `Promise.all` 里已有 `fetchExtremeEvents`，再加 `loadForecast(selectedWeatherPointId.value)`。`watch(selectedWeatherPointId)` 里与 `loadThresholds` 一起调 `loadForecast`。

模板：在 `weather-extreme-tags` 与快照卡片之间（或卡片下方）加表，无行时显示「暂无该站 7 日预报」：

```vue
<a-table
  v-if="forecastDays.length"
  class="forecast-table"
  size="small"
  :pagination="false"
  :data-source="forecastDays"
  :columns="forecastColumns"
  row-key="date" />
```

列：日期、最高温 ℃、最低温 ℃、降水 mm、风速 m/s。`forecastColumns` 用中文 title。

把现有 `a-tag` 包成可点击：

```vue
<a-tag
  v-for="title in activeExtremeTitles"
  :key="title"
  color="orange"
  style="cursor: pointer"
  @click="router.push('/warnings')">
  {{ title }}
</a-tag>
```

- [ ] **Step 3: 字幕**

气象 Tab 的 `subtitle` 现为「土壤墒情与局地小气候实时监测」，有 7 日数据时改为带「含 7 日预报」。

- [ ] **Step 4: 类型检查**

Run: `pnpm exec vue-tsc --noEmit`

Expected: 无新增错误。

---

### Task 3: 手工验收（不写自动化 E2E）

- [ ] Mock 已启动时打开相关数据 → 气象 → 选「监测站 · 雄县」：表有 7 行，22 日最高温 41。
- [ ] 若链 2 已跑过且 `extremeEvents` 有「极端高温」，橙色标签可点进 `/warnings`。
- [ ] 切到没有预报的站：表空、不报错。

---

## 本阶段不做

和风/彩云真实 API；新建 `useWeatherForecast.ts`；地图右侧 24 h 迷你趋势（P1 抽屉）；改链 2 阈值。
