# 23 类训后后端对齐与推理加厚 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 23 类权重已就位的前提下，把推理服务、防治库、作物约束和预警病名对齐，使智能分析返回 top3 / `needs_review` / `model_version` / 防治片段，且水稻与新增玉米类不再空白或串作物。

**Architecture:** 继续用 Flask `ml-bjj/serving`（5000）做唯一推理进程，Mock（3000）只管业务 CRUD。新增纯函数模块处理作物掩码与 topk/复核判定，避免把逻辑堆进 `app.py`。`treatments.json` 仍是知识库唯一源；Flask 增加只读防治接口，前端 P0/P1 继续读静态副本（`sync:knowledge`）。

**Tech Stack:** Flask 3 + flask-cors、PyTorch/timm（已有）、pytest、Vue 3 / Ant Design Vue、Vite 代理、json-server Mock。

**Spec:** [`新模型训后-后端丰富说明.md`](./新模型训后-后端丰富说明.md) V2.0（P0 + P1）。

## Global Constraints

- 类别键名必须与 `ml-bjj/models/pest-cls-meta.json` 的 `classes` 逐字一致，共 23 个（见下方 CANONICAL_CLASSES）。
- 作物过滤：只保留「该作物病害 ∪ 健康」，置零后重新归一化；未知 `cropType` 不过滤。
- `needs_review`：`confidence < 0.7` 或 top1−top2 `< 0.10`。
- 缺防治条目时返回「暂无条目」对象，禁止回落为「健康」。
- `details.engine` 使用 `"bjj-23"`（Mock 时为 `"mock"`）。
- 不实现 batch / history / feedback / ONNX / gunicorn / 规则链 / 分析页三列改版。
- 不把训练脚本迁入 serving；不改 git config。
- 测试在项目根执行：先 `ml-bjj\.venv\Scripts\Activate.ps1`。

### Canonical classes and crop map

```python
CANONICAL_CLASSES = [
    "健康",
    "小麦锈病", "小麦赤霉病", "小麦白粉病", "小麦蚜虫为害",
    "玉米大斑病", "玉米锈病", "玉米南方锈病", "玉米小斑病",
    "玉米弯孢叶斑病", "玉米褐斑病", "玉米瘤黑粉病", "玉米茎腐病", "玉米穗腐病",
    "番茄早疫病",
    "水稻白叶枯病", "水稻褐斑病", "水稻负泥虫为害", "稻瘟病",
    "水稻叶鞘腐败病", "水稻叶黑粉病", "水稻窄条斑病", "稻颈瘟",
]

CROP_CLASS_GROUPS = {
    "wheat": {"健康", "小麦锈病", "小麦赤霉病", "小麦白粉病", "小麦蚜虫为害"},
    "corn": {
        "健康", "玉米大斑病", "玉米锈病", "玉米南方锈病", "玉米小斑病",
        "玉米弯孢叶斑病", "玉米褐斑病", "玉米瘤黑粉病", "玉米茎腐病", "玉米穗腐病",
    },
    "tomato": {"健康", "番茄早疫病"},
    "rice": {
        "健康", "水稻白叶枯病", "水稻褐斑病", "水稻负泥虫为害", "稻瘟病",
        "水稻叶鞘腐败病", "水稻叶黑粉病", "水稻窄条斑病", "稻颈瘟",
    },
}
```

### File map

| File | Responsibility |
|------|----------------|
| `ml-bjj/serving/crop_filter.py` | 23 类常量、作物集合、`mask_and_renorm` |
| `ml-bjj/serving/predict_utils.py` | `rank_topk`、`needs_review` |
| `ml-bjj/serving/knowledge.py` | 读取 `treatments.json`、`get_treatment_item` |
| `ml-bjj/serving/inference.py` | `predict` 保持兼容；新增 `predict_detailed` |
| `ml-bjj/serving/app.py` | `/health` 加厚、分析响应加厚、防治 GET、作物过滤 |
| `ml-bjj/knowledge/treatments.json` | 23 类防治源 |
| `src/assets/knowledge/treatments.json` | 由 `pnpm run sync:knowledge` 生成，勿手改 |
| `ml-bjj/tests/` | pytest |
| `vite.config.ts` | `/api/treatments` → 5000 |
| `src/views/user/DataAnalysis.vue` | 水稻选项；使用 serving 的 `level` |
| `src/composables/useTreatmentGuide.ts` | 未知类不回落「健康」 |
| `src/mock/db.json` | AI 预警样例改为 23 类中文病名 |

---

### Task 1: 作物掩码纯函数

**Files:**
- Create: `ml-bjj/serving/crop_filter.py`
- Create: `ml-bjj/tests/conftest.py`
- Create: `ml-bjj/tests/test_crop_filter.py`
- Test: `ml-bjj/tests/test_crop_filter.py`

**Interfaces:**
- Consumes: 无
- Produces: `CANONICAL_CLASSES: list[str]`；`CROP_CLASS_GROUPS: dict[str, set[str]]`；`classes_for_crop(crop_type: str) -> set[str] | None`（未知作物返回 `None`）；`mask_and_renorm(probs: list[float], classes: list[str], crop_type: str) -> list[float]`

- [ ] **Step 1: Write the failing tests**

Create `ml-bjj/tests/conftest.py`:

```python
from __future__ import annotations

import sys
from pathlib import Path

SERVE_DIR = Path(__file__).resolve().parents[1] / "serving"
if str(SERVE_DIR) not in sys.path:
    sys.path.insert(0, str(SERVE_DIR))
```

Create `ml-bjj/tests/test_crop_filter.py`:

```python
from crop_filter import (
    CANONICAL_CLASSES,
    CROP_CLASS_GROUPS,
    classes_for_crop,
    mask_and_renorm,
)


def test_canonical_count_is_23():
    assert len(CANONICAL_CLASSES) == 23
    assert len(set(CANONICAL_CLASSES)) == 23


def test_every_group_is_subset_of_canonical():
    canonical = set(CANONICAL_CLASSES)
    for labels in CROP_CLASS_GROUPS.values():
        assert labels <= canonical
        assert "健康" in labels


def test_unknown_crop_returns_none():
    assert classes_for_crop("unknown") is None
    assert classes_for_crop("") is None


def test_wheat_zeros_rice_then_renorms():
    classes = ["小麦锈病", "稻瘟病", "健康"]
    probs = [0.2, 0.7, 0.1]
    out = mask_and_renorm(probs, classes, "wheat")
    assert out[1] == 0.0
    assert abs(sum(out) - 1.0) < 1e-9
    assert abs(out[0] - 0.2 / 0.3) < 1e-9
    assert abs(out[2] - 0.1 / 0.3) < 1e-9


def test_unknown_crop_does_not_filter():
    classes = ["小麦锈病", "稻瘟病"]
    probs = [0.4, 0.6]
    assert mask_and_renorm(probs, classes, "peach") == probs
```

- [ ] **Step 2: Run test to verify it fails**

Run:

```powershell
cd D:\code2\software\vue\program\DetectSystem
ml-bjj\.venv\Scripts\Activate.ps1
pip install pytest
if (-not (Select-String -Path ml-bjj\requirements.txt -Pattern '^pytest' -Quiet)) { Add-Content ml-bjj\requirements.txt "`npytest>=8.0.0" }
pytest ml-bjj/tests/test_crop_filter.py -v
```

Expected: FAIL with `ModuleNotFoundError: No module named 'crop_filter'`

- [ ] **Step 3: Write minimal implementation**

Create `ml-bjj/serving/crop_filter.py` with the Canonical classes and crop map from Global Constraints, plus:

```python
from __future__ import annotations


def classes_for_crop(crop_type: str) -> set[str] | None:
    return CROP_CLASS_GROUPS.get(crop_type)


def mask_and_renorm(probs: list[float], classes: list[str], crop_type: str) -> list[float]:
    allowed = classes_for_crop(crop_type)
    if allowed is None or len(probs) != len(classes):
        return list(probs)
    masked = [p if label in allowed else 0.0 for p, label in zip(probs, classes)]
    total = sum(masked)
    if total <= 0:
        return list(probs)
    return [p / total for p in masked]
```

Put `CANONICAL_CLASSES` and `CROP_CLASS_GROUPS` at module top exactly as in Global Constraints.

- [ ] **Step 4: Run tests to verify they pass**

Run: `pytest ml-bjj/tests/test_crop_filter.py -v`

Expected: PASS (5 passed)

- [ ] **Step 5: Commit**

```bash
git add ml-bjj/serving/crop_filter.py ml-bjj/tests/conftest.py ml-bjj/tests/test_crop_filter.py ml-bjj/requirements.txt
git commit -m "feat(serving): add 23-class crop mask and renorm helper"
```

---

### Task 2: topk 与 needs_review 纯函数

**Files:**
- Create: `ml-bjj/serving/predict_utils.py`
- Test: `ml-bjj/tests/test_predict_utils.py`

**Interfaces:**
- Consumes: `crop_filter.CANONICAL_CLASSES` 不直接依赖
- Produces: `CONF_REVIEW_THRESHOLD: float = 0.7`；`TOP2_MARGIN: float = 0.10`；`rank_topk(probs: list[float], classes: list[str], k: int = 3) -> list[dict[str, str | float]]`（每项含 `label: str`、`confidence: float`，按 confidence 降序）；`needs_review(topk: list[dict[str, str | float]], confidence: float) -> bool`

- [ ] **Step 1: Write the failing test**

Create `ml-bjj/tests/test_predict_utils.py`:

```python
from predict_utils import needs_review, rank_topk


def test_rank_topk_orders_and_clips_to_three():
    classes = ["A", "B", "C", "D"]
    probs = [0.05, 0.60, 0.25, 0.10]
    top = rank_topk(probs, classes, k=3)
    assert [item["label"] for item in top] == ["B", "C", "D"]
    assert top[0]["confidence"] == 0.60
    assert len(top) == 3


def test_needs_review_when_confidence_low():
    top = [{"label": "稻瘟病", "confidence": 0.55}, {"label": "水稻褐斑病", "confidence": 0.20}]
    assert needs_review(top, 0.55) is True


def test_needs_review_when_margin_small():
    top = [{"label": "稻瘟病", "confidence": 0.46}, {"label": "水稻褐斑病", "confidence": 0.44}]
    assert needs_review(top, 0.46) is True


def test_needs_review_false_when_confident_and_separated():
    top = [{"label": "小麦锈病", "confidence": 0.91}, {"label": "小麦白粉病", "confidence": 0.05}]
    assert needs_review(top, 0.91) is False
```

- [ ] **Step 2: Run test to verify it fails**

Run: `pytest ml-bjj/tests/test_predict_utils.py -v`

Expected: FAIL with `ModuleNotFoundError: No module named 'predict_utils'`

- [ ] **Step 3: Write minimal implementation**

Create `ml-bjj/serving/predict_utils.py`:

```python
from __future__ import annotations

CONF_REVIEW_THRESHOLD = 0.7
TOP2_MARGIN = 0.10


def rank_topk(probs: list[float], classes: list[str], k: int = 3) -> list[dict[str, str | float]]:
    paired = sorted(zip(classes, probs), key=lambda item: item[1], reverse=True)
    return [{"label": label, "confidence": float(conf)} for label, conf in paired[:k]]


def needs_review(topk: list[dict[str, str | float]], confidence: float) -> bool:
    if confidence < CONF_REVIEW_THRESHOLD:
        return True
    if len(topk) >= 2:
        top1 = float(topk[0]["confidence"])
        top2 = float(topk[1]["confidence"])
        if (top1 - top2) < TOP2_MARGIN:
            return True
    return False
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `pytest ml-bjj/tests/test_predict_utils.py -v`

Expected: PASS (4 passed)

- [ ] **Step 5: Commit**

```bash
git add ml-bjj/serving/predict_utils.py ml-bjj/tests/test_predict_utils.py
git commit -m "feat(serving): add topk ranking and needs_review rules"
```

---

### Task 3: 防治库覆盖测试（先红）

**Files:**
- Create: `ml-bjj/serving/knowledge.py`（本任务只写加载与查找；完整 23 条目在 Task 4）
- Create: `ml-bjj/tests/test_treatments_coverage.py`
- Test: `ml-bjj/tests/test_treatments_coverage.py`

**Interfaces:**
- Consumes: `crop_filter.CANONICAL_CLASSES`
- Produces: `load_catalog() -> dict`；`get_treatment_item(label: str) -> tuple[dict, bool]`（第二项 `found`；未找到时返回降级 dict，`found=False`，且 `summary` 含「暂无」与原 `label`，`crop` 为 `"通用"`）

- [ ] **Step 1: Write the failing tests**

Create `ml-bjj/tests/test_treatments_coverage.py`:

```python
from crop_filter import CANONICAL_CLASSES
from knowledge import get_treatment_item, load_catalog


def test_catalog_covers_all_canonical_labels():
    items = load_catalog()["items"]
    missing = [label for label in CANONICAL_CLASSES if label not in items]
    assert missing == [], f"treatments.json 缺少: {missing}"


def test_each_item_has_summary_and_measures():
    items = load_catalog()["items"]
    for label in CANONICAL_CLASSES:
        item = items[label]
        assert item["summary"].strip()
        assert "measures" in item


def test_missing_label_does_not_fallback_to_healthy():
    item, found = get_treatment_item("不存在的病名XYZ")
    assert found is False
    assert item["crop"] == "通用"
    assert "暂无" in item["summary"]
    assert "不存在的病名XYZ" in item["summary"]
    assert item is not load_catalog()["items"]["健康"]
```

- [ ] **Step 2: Run test to verify it fails**

Run: `pytest ml-bjj/tests/test_treatments_coverage.py -v`

Expected: FAIL with `ModuleNotFoundError: No module named 'knowledge'`（若已误创建 knowledge 但未扩库，则 FAIL 在 `treatments.json 缺少`）

- [ ] **Step 3: Write knowledge loader only**

Create `ml-bjj/serving/knowledge.py`:

```python
from __future__ import annotations

import json
from pathlib import Path

KNOWLEDGE_PATH = Path(__file__).resolve().parents[1] / "knowledge" / "treatments.json"


def load_catalog() -> dict:
    return json.loads(KNOWLEDGE_PATH.read_text(encoding="utf-8"))


def get_treatment_item(label: str) -> tuple[dict, bool]:
    items = load_catalog().get("items", {})
    if label in items:
        return items[label], True
    fallback = {
        "crop": "通用",
        "crop_en": "general",
        "aliases": [],
        "summary": f"暂无「{label}」的防治条目，请以田间复核与当地植保意见为准。",
        "risk_level": "medium",
        "symptoms": [],
        "measures": {
            "chemical": [],
            "biological": [],
            "agronomic": ["保留清晰样本照片与拍摄时间，送农技员复核后再用药。"],
        },
        "timing": "",
        "safety": "在确认病名之前不要盲目施药。",
        "references": [],
    }
    return fallback, False
```

Do not expand `treatments.json` in this step.

- [ ] **Step 4: Re-run coverage test — still fail on missing keys**

Run: `pytest ml-bjj/tests/test_treatments_coverage.py::test_catalog_covers_all_canonical_labels -v`

Expected: FAIL with `treatments.json 缺少:` and a list including `稻瘟病`、`玉米南方锈病` 等。`test_missing_label_does_not_fallback_to_healthy` 应已 PASS。

- [ ] **Step 5: Commit loader + red coverage test**

```bash
git add ml-bjj/serving/knowledge.py ml-bjj/tests/test_treatments_coverage.py
git commit -m "test(knowledge): require treatments.json to cover all 23 classes"
```

---

### Task 4: 扩 treatments.json 到 23 类并同步前端

**Files:**
- Modify: `ml-bjj/knowledge/treatments.json`
- Modify: `src/assets/knowledge/treatments.json`（仅通过 sync 脚本）
- Test: `ml-bjj/tests/test_treatments_coverage.py`

**Interfaces:**
- Consumes: `load_catalog` / `CANONICAL_CLASSES`
- Produces: `items` 含全部 23 个键；`version` 为 `"2.0"`；`updated_at` 为 `"2026-08-20"`；已有 8 类正文保留不删

- [ ] **Step 1: Confirm coverage test is still red**

Run: `pytest ml-bjj/tests/test_treatments_coverage.py::test_catalog_covers_all_canonical_labels -v`

Expected: FAIL listing missing corn/rice keys.

- [ ] **Step 2: Extend the JSON**

In `ml-bjj/knowledge/treatments.json`:

1. Set `"version": "2.0"` and `"updated_at": "2026-08-20"`.
2. Keep existing keys `健康`、`小麦锈病`、`小麦赤霉病`、`小麦白粉病`、`小麦蚜虫为害`、`玉米大斑病`、`玉米锈病`、`番茄早疫病` unchanged except they remain valid.
3. Insert the following keys into `items` (same nesting as `玉米锈病`):

```json
    "玉米南方锈病": {
      "crop": "玉米",
      "crop_en": "corn",
      "aliases": ["南方锈病", "玉米南方叶锈"],
      "summary": "玉米南方锈病以叶部橙黄色夏孢子堆为主，高温高湿年份扩展快，可造成叶片早枯。",
      "risk_level": "high",
      "symptoms": [
        "叶片密布橙黄至铁锈色小疱斑，用手轻抹可见粉状孢子",
        "下部叶片先发，严重时全株叶片枯黄"
      ],
      "measures": {
        "chemical": [
          "发病初期选用戊唑醇、吡唑醚菌酯等登记药剂喷雾",
          "流行年份间隔 7–10 天复喷，注意与普通锈病统筹防治"
        ],
        "biological": ["保护天敌，避免与广谱杀虫剂不必要混用"],
        "agronomic": [
          "选用抗病品种，合理密植改善通风",
          "清除自生苗与田间杂草，减少桥梁寄主"
        ]
      },
      "timing": "抽雄前后为重点监测期；见病即防，不可等全田黄化。",
      "safety": "遵守安全间隔期；大风天停止喷雾。",
      "references": ["玉米南方锈病识别与防控要点（团队整理）"]
    },
    "玉米小斑病": {
      "crop": "玉米",
      "crop_en": "corn",
      "aliases": ["小斑病", "玉米南方叶枯"],
      "summary": "玉米小斑病病斑较细小、椭圆形，潮湿时可见霉层，高温高湿下可造成叶片枯死。",
      "risk_level": "medium",
      "symptoms": [
        "叶片出现黄褐至灰褐色椭圆形小斑，边缘较清晰",
        "湿度大时病斑上可产生暗色霉状物"
      ],
      "measures": {
        "chemical": [
          "发病初期选用苯醚甲环唑、代森锰锌等登记药剂喷雾",
          "可与大斑病防治结合，避免重复打药"
        ],
        "biological": [],
        "agronomic": [
          "选用抗病品种，避免偏施氮肥",
          "收获后深翻病残体"
        ]
      },
      "timing": "苗期至抽雄期见病即防；低洼高湿田块优先巡查。",
      "safety": "施药注意个人防护，远离水产养殖区。",
      "references": ["玉米叶斑类病害综合防控（团队整理）"]
    },
    "玉米弯孢叶斑病": {
      "crop": "玉米",
      "crop_en": "corn",
      "aliases": ["弯孢叶斑", "黄斑病"],
      "summary": "弯孢叶斑病多在中后期发生，病斑近圆形、黄褐，严重时叶片早衰影响灌浆。",
      "risk_level": "medium",
      "symptoms": [
        "叶片出现近圆形黄褐色斑，中央可褪色",
        "病斑可愈合导致叶肉枯死"
      ],
      "measures": {
        "chemical": [
          "发病初期选用吡唑醚菌酯、苯醚甲环唑等登记药剂",
          "重病田块 7–10 天复喷 1 次"
        ],
        "biological": [],
        "agronomic": [
          "合理密植，增施钾肥提高抗性",
          "及时清除下部病叶并带出田外"
        ]
      },
      "timing": "大喇叭口至灌浆期监测；连阴雨后及时巡田。",
      "safety": "轮换作用机制，延缓抗药性。",
      "references": ["玉米弯孢叶斑病防治要点（团队整理）"]
    },
    "玉米褐斑病": {
      "crop": "玉米",
      "crop_en": "corn",
      "aliases": ["褐斑病", "玉米褐斑"],
      "summary": "玉米褐斑病常见于叶鞘与叶片交接处，病斑褐色隆起，可影响茎叶养分运输。",
      "risk_level": "medium",
      "symptoms": [
        "叶鞘或叶片出现褐色近圆形斑点，表面可粗糙隆起",
        "严重时病斑密集，叶片局部枯黄"
      ],
      "measures": {
        "chemical": [
          "发病初期选用三唑类或代森锰锌等登记药剂喷雾",
          "重点喷到叶鞘与中下部叶片"
        ],
        "biological": [],
        "agronomic": [
          "轮作倒茬，减少连作菌源",
          "避免田间长期积水"
        ]
      },
      "timing": "拔节至抽雄期见病防治。",
      "safety": "施药后田间挂牌，遵守安全间隔期。",
      "references": ["玉米褐斑病识别与防治（团队整理）"]
    },
    "玉米瘤黑粉病": {
      "crop": "玉米",
      "crop_en": "corn",
      "aliases": ["黑粉病", "玉米瘤黑粉"],
      "summary": "瘤黑粉病在茎、叶、穗等部位形成肿瘤，成熟后破裂散出黑粉，直接损害产量。",
      "risk_level": "high",
      "symptoms": [
        "植株出现灰白至黑色肿瘤，内部充满黑粉",
        "果穗、茎节受害后畸形或空秆"
      ],
      "measures": {
        "chemical": [
          "以预防为主，播种前选用登记种衣剂处理种子",
          "田间出现新鲜肿瘤可在破裂前人工摘除并带出销毁，勿随意丢弃"
        ],
        "biological": [],
        "agronomic": [
          "选用抗病品种，避免在病田连作",
          "平衡施肥，减少机械伤口"
        ]
      },
      "timing": "苗期至抽雄期巡查肿瘤；收获后清除病残。",
      "safety": "摘除肿瘤时戴手套，避免黑粉吸入。",
      "references": ["玉米瘤黑粉病防控技术要点（团队整理）"]
    },
    "玉米茎腐病": {
      "crop": "玉米",
      "crop_en": "corn",
      "aliases": ["茎基腐", "青枯病"],
      "summary": "茎腐病导致茎基变软、植株青枯或黄枯倒伏，灌浆期遇高温高湿更重。",
      "risk_level": "high",
      "symptoms": [
        "茎基部变褐变软，按压易倒",
        "叶片自上而下青枯或黄枯，果穗下垂"
      ],
      "measures": {
        "chemical": [
          "发病初期可选用登记杀菌剂灌根或喷茎基，严格按标签使用",
          "与种衣剂预防结合，单靠后期喷雾效果有限"
        ],
        "biological": ["有条件时选用芽孢杆菌类菌剂拌种或灌根（须为登记产品）"],
        "agronomic": [
          "选用抗茎腐品种，避免田间积水",
          "收获后深翻，减少病残体"
        ]
      },
      "timing": "抽雄至灌浆期重点监测倒伏与青枯株。",
      "safety": "灌根作业避免药液流入沟渠。",
      "references": ["玉米茎腐病综合防控（团队整理）"]
    },
    "玉米穗腐病": {
      "crop": "玉米",
      "crop_en": "corn",
      "aliases": ["穗腐", "果穗霉变"],
      "summary": "穗腐病造成果穗霉变、籽粒腐烂，潮湿收获期风险高，并可能伴随真菌毒素风险。",
      "risk_level": "high",
      "symptoms": [
        "果穗花丝或籽粒出现粉红、青绿或黑色霉层",
        "病穗轻、籽粒干瘪或腐烂"
      ],
      "measures": {
        "chemical": [
          "抽雄吐丝期可结合叶斑病防治选用登记杀菌剂保护果穗",
          "已严重霉变果穗应剔除，不宜作为防治后继续食用的依据"
        ],
        "biological": [],
        "agronomic": [
          "适期收获，及时晾晒降水",
          "避免虫伤与机械伤口，降低侵染"
        ]
      },
      "timing": "吐丝至收获期监测；连阴雨年份提前抢收。",
      "safety": "霉变籽粒不得饲喂或加工食用；注意毒素风险。",
      "references": ["玉米穗腐病与收获期管理（团队整理）"]
    },
    "水稻白叶枯病": {
      "crop": "水稻",
      "crop_en": "rice",
      "aliases": ["白叶枯", "白叶枯病"],
      "summary": "白叶枯病是细菌性维管束病害，叶缘出现黄白条斑，暴雨大风后易流行。",
      "risk_level": "high",
      "symptoms": [
        "叶缘出现黄白色条斑，可向内扩展使叶片枯白",
        "湿度大时病部可有菌脓"
      ],
      "measures": {
        "chemical": [
          "发病初期选用噻菌铜、叶枯唑等登记药剂喷雾",
          "台风暴雨后及时补防"
        ],
        "biological": [],
        "agronomic": [
          "选用抗病品种，浅水勤灌，避免串灌",
          "减少田间伤口作业"
        ]
      },
      "timing": "分蘖至孕穗期为重点；暴雨后 24–48 小时内巡田。",
      "safety": "药剂远离桑园与鱼塘；遵守安全间隔期。",
      "references": ["水稻白叶枯病防控技术意见（团队整理）"]
    },
    "水稻褐斑病": {
      "crop": "水稻",
      "crop_en": "rice",
      "aliases": ["褐斑病", "胡麻斑病"],
      "summary": "褐斑病在叶片形成褐色椭圆形斑，缺肥、酸性土及缺水田块更重。",
      "risk_level": "medium",
      "symptoms": [
        "叶片散生褐色椭圆形小斑，中央灰褐",
        "严重时病斑愈合，叶片枯黄"
      ],
      "measures": {
        "chemical": [
          "发病初期选用三环唑、戊唑醇等登记药剂（按标签兼治叶部病害）"
        ],
        "biological": [],
        "agronomic": [
          "平衡施肥，纠正缺钾与缺硅",
          "避免长期深水或严重干旱交替"
        ]
      },
      "timing": "分蘖至抽穗期见病防治。",
      "safety": "与稻瘟病防治统筹，避免重复用药。",
      "references": ["水稻叶部斑点类病害识别（团队整理）"]
    },
    "水稻负泥虫为害": {
      "crop": "水稻",
      "crop_en": "rice",
      "aliases": ["负泥虫", "水稻负泥虫"],
      "summary": "负泥虫幼虫取食叶肉，残留白色条纹，分蘖期受害影响分蘖与光合。",
      "risk_level": "medium",
      "symptoms": [
        "叶片出现白色纵行食痕，叶肉被刮食",
        "可见背负粪泥的幼虫或成虫"
      ],
      "measures": {
        "chemical": [
          "达到防治指标时选用阿维菌素等登记杀虫剂，注意对天敌影响"
        ],
        "biological": [
          "保护青蛙、蜘蛛等天敌，优先人工扫除高密度田块"
        ],
        "agronomic": [
          "清除田边杂草，减少虫源",
          "合理灌溉，避免长期积水过深"
        ]
      },
      "timing": "秧田至分蘖期为重点监测窗口。",
      "safety": "施药避开蜜蜂活动高峰；远离水产养殖区。",
      "references": ["水稻负泥虫防治要点（团队整理）"]
    },
    "稻瘟病": {
      "crop": "水稻",
      "crop_en": "rice",
      "aliases": ["稻瘟", "叶瘟"],
      "summary": "稻瘟病可表现为叶瘟、节瘟等，典型病斑梭形、中央灰白、边缘褐色，流行年份减产明显。",
      "risk_level": "high",
      "symptoms": [
        "叶片梭形病斑，中央灰白，边缘褐色，外有黄晕",
        "潮湿时病部可见灰绿色霉层"
      ],
      "measures": {
        "chemical": [
          "发病初期选用三环唑、稻瘟灵、春雷霉素等登记药剂",
          "破口前 5–7 天预防穗颈瘟，与稻颈瘟防治一体安排"
        ],
        "biological": ["优先使用生物源药剂与抗性品种"],
        "agronomic": [
          "选用抗病品种，避免偏施氮肥",
          "浅水勤灌，改善通风透光"
        ]
      },
      "timing": "分蘖盛期防叶瘟；破口抽穗期防穗瘟。",
      "safety": "严格安全间隔期；雨前抢喷、雨后补喷。",
      "references": ["水稻稻瘟病防控技术方案（团队整理）"]
    },
    "水稻叶鞘腐败病": {
      "crop": "水稻",
      "crop_en": "rice",
      "aliases": ["叶鞘腐败", "鞘腐病"],
      "summary": "叶鞘腐败病危害剑叶叶鞘，造成穗抽出困难或秕谷增加。",
      "risk_level": "medium",
      "symptoms": [
        "剑叶叶鞘出现不规则褐斑，可向内扩展",
        "严重时穗抽出受阻，穗部褐变"
      ],
      "measures": {
        "chemical": [
          "孕穗至破口期结合穗期病害选用戊唑醇等登记药剂"
        ],
        "biological": [],
        "agronomic": [
          "合理密植，避免田间郁闭",
          "适期晒田，降低湿度"
        ]
      },
      "timing": "孕穗至抽穗期为重点。",
      "safety": "与稻瘟、纹枯类防治统筹，轮换药剂。",
      "references": ["水稻叶鞘腐败病防治要点（团队整理）"]
    },
    "水稻叶黑粉病": {
      "crop": "水稻",
      "crop_en": "rice",
      "aliases": ["叶黑粉", "叶黑粉病"],
      "summary": "叶黑粉病在叶片上形成黑色短线条状孢子堆，一般对产量影响轻于稻瘟，但需与稻瘟区分。",
      "risk_level": "low",
      "symptoms": [
        "叶片沿叶脉出现黑色短线状突起",
        "严重时叶片局部枯黄"
      ],
      "measures": {
        "chemical": [
          "一般不单独用药；若与稻瘟混发，按稻瘟病方案防治"
        ],
        "biological": [],
        "agronomic": [
          "选用抗病品种，清除病残体",
          "平衡施肥，避免贪青晚熟"
        ]
      },
      "timing": "分蘖至抽穗期结合叶瘟调查一并识别。",
      "safety": "避免把叶黑粉误当稻瘟而过量施药。",
      "references": ["水稻叶黑粉病与稻瘟病田间区分（团队整理）"]
    },
    "水稻窄条斑病": {
      "crop": "水稻",
      "crop_en": "rice",
      "aliases": ["窄条斑", "细菌性条斑"],
      "summary": "窄条斑病为细菌性病害，叶片出现暗绿至黄褐细条斑，可与白叶枯混淆，需结合条斑宽度判断。",
      "risk_level": "medium",
      "symptoms": [
        "叶片出现对光半透明的细条斑，后变黄褐",
        "条斑较白叶枯更细，可布满叶片"
      ],
      "measures": {
        "chemical": [
          "发病初期选用噻菌铜、叶枯唑等登记药剂",
          "暴雨后及时喷雾保护"
        ],
        "biological": [],
        "agronomic": [
          "避免串灌，减少菌源扩散",
          "选用抗病品种"
        ]
      },
      "timing": "分蘖至孕穗期；台风暴雨后立即巡查。",
      "safety": "与白叶枯病药剂方案接近，注意标签登记作物。",
      "references": ["水稻细菌性条斑病防控要点（团队整理）"]
    },
    "稻颈瘟": {
      "crop": "水稻",
      "crop_en": "rice",
      "aliases": ["穗颈瘟", "颈瘟"],
      "summary": "稻颈瘟发生在穗颈，可造成白穗或秕谷，是稻瘟病的穗期类型，损失往往大于叶瘟。",
      "risk_level": "high",
      "symptoms": [
        "穗颈变褐、缢缩，穗部下垂成白穗",
        "枝梗、谷粒亦可出现褐斑"
      ],
      "measures": {
        "chemical": [
          "破口前 5–7 天与齐穗期各防 1 次，药剂同稻瘟病登记品种",
          "已出现白穗以减少再侵染和保护未病穗为主，无法恢复已枯穗"
        ],
        "biological": [],
        "agronomic": [
          "破口期避免偏施氮肥和长期深水",
          "叶瘟发生田块必须落实穗期预防"
        ]
      },
      "timing": "破口至齐穗是不可错过的窗口。",
      "safety": "穗期用药严格遵守安全间隔与水质保护要求。",
      "references": ["水稻穗颈瘟预防技术要点（团队整理）"]
    }
```

JSON must remain valid (commas between objects). Do not change the top-level `disclaimer`.

- [ ] **Step 3: Sync frontend copy**

Run:

```powershell
pnpm run sync:knowledge
```

Expected stdout contains: `ml-bjj/knowledge/treatments.json → src/assets/knowledge/treatments.json`

- [ ] **Step 4: Run tests to verify they pass**

Run: `pytest ml-bjj/tests/test_treatments_coverage.py -v`

Expected: PASS (3 passed)

- [ ] **Step 5: Commit**

```bash
git add ml-bjj/knowledge/treatments.json src/assets/knowledge/treatments.json
git commit -m "feat(knowledge): cover all 23 disease classes in treatments.json"
```

---

### Task 5: `predict_detailed` 接入作物过滤与 topk

**Files:**
- Modify: `ml-bjj/serving/inference.py`
- Test: `ml-bjj/tests/test_predict_detailed.py`

**Interfaces:**
- Consumes: `mask_and_renorm`；`rank_topk`；`needs_review`
- Produces: `PredictResult` dataclass：`label: str`、`confidence: float`、`topk: list[dict[str, str | float]]`、`needs_review: bool`；`PestClassifier.predict_detailed(image: Image.Image, crop_type: str = "unknown") -> PredictResult`；`PestClassifier.predict` 仍返回 `tuple[str, float]`（内部调用 `predict_detailed`）

- [ ] **Step 1: Write the failing test**

Create `ml-bjj/tests/test_predict_detailed.py`:

```python
from PIL import Image
import torch

from inference import PestClassifier, PredictResult


class _FakeModel:
    def __init__(self, logits: torch.Tensor) -> None:
        self.logits = logits

    def eval(self):
        return self

    def __call__(self, tensor):
        return self.logits


def test_predict_detailed_applies_wheat_mask():
    clf = PestClassifier.__new__(PestClassifier)
    clf.classes = ["小麦锈病", "稻瘟病", "健康"]
    clf.transform = lambda img: torch.zeros(3, 224, 224)
    clf.model = _FakeModel(torch.tensor([[1.0, 8.0, 0.5]]))
    img = Image.new("RGB", (16, 16), color=(0, 128, 0))
    result = clf.predict_detailed(img, crop_type="wheat")
    assert isinstance(result, PredictResult)
    assert result.label in {"小麦锈病", "健康"}
    assert result.label != "稻瘟病"
    assert result.topk[0]["label"] == result.label


def test_predict_wrapper_returns_tuple():
    clf = PestClassifier.__new__(PestClassifier)
    clf.classes = ["健康", "小麦锈病"]
    clf.transform = lambda img: torch.zeros(3, 224, 224)
    clf.model = _FakeModel(torch.tensor([[3.0, 0.1]]))
    img = Image.new("RGB", (8, 8), color=(1, 1, 1))
    label, conf = clf.predict(img)
    assert label == "健康"
    assert conf > 0.5
```

- [ ] **Step 2: Run test to verify it fails**

Run: `pytest ml-bjj/tests/test_predict_detailed.py -v`

Expected: FAIL with `ImportError` or `AttributeError: type object 'PestClassifier' has no attribute 'predict_detailed'`

- [ ] **Step 3: Implement `predict_detailed`**

Modify `ml-bjj/serving/inference.py`:

1. Add imports:

```python
from dataclasses import dataclass

from crop_filter import mask_and_renorm
from predict_utils import needs_review, rank_topk
```

2. Add dataclass above `PestClassifier`:

```python
@dataclass
class PredictResult:
    label: str
    confidence: float
    topk: list[dict[str, str | float]]
    needs_review: bool
```

3. Replace `predict` with:

```python
    def predict_detailed(self, image: Image.Image, crop_type: str = "unknown") -> PredictResult:
        img = image.convert("RGB")
        tensor = self.transform(img).unsqueeze(0)
        with torch.no_grad():
            logits = self.model(tensor)
            probs = torch.softmax(logits, dim=1)[0].tolist()
        filtered = mask_and_renorm(probs, self.classes, crop_type)
        topk = rank_topk(filtered, self.classes, k=3)
        label = str(topk[0]["label"])
        confidence = float(topk[0]["confidence"])
        return PredictResult(
            label=label,
            confidence=confidence,
            topk=topk,
            needs_review=needs_review(topk, confidence),
        )

    def predict(self, image: Image.Image, crop_type: str = "unknown") -> tuple[str, float]:
        result = self.predict_detailed(image, crop_type)
        return result.label, result.confidence
```

Keep `PestClassifier.__init__` unchanged.

- [ ] **Step 4: Run tests to verify they pass**

Run: `pytest ml-bjj/tests/test_predict_detailed.py ml-bjj/tests/test_crop_filter.py ml-bjj/tests/test_predict_utils.py -v`

Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add ml-bjj/serving/inference.py ml-bjj/tests/test_predict_detailed.py
git commit -m "feat(serving): return topk and apply crop filter during predict"
```

---

### Task 6: 加厚 `/health` 与 `POST /api/analysis/image`，新增防治 GET

**Files:**
- Modify: `ml-bjj/serving/app.py`
- Modify: `vite.config.ts`
- Test: `ml-bjj/tests/test_app_api.py`

**Interfaces:**
- Consumes: `get_classifier().predict_detailed`；`get_treatment_item`；`resolve_weights_path`；`ml-bjj/models/pest-cls-meta.json`
- Produces:
  - `GET /health` JSON: `status`、`mock`、`classes_count`、`classes`、`model_version`（含 `trained_at`、`best_val_acc`、`classes_count`）、`weights_mtime`（文件存在时为 ISO 字符串，否则 `null`）、`cuda`、`engine`（`"bjj-23"` 或 `"mock"`）
  - `POST /api/analysis/image` 在原字段上增加 `topk`、`model_version`、`treatment`、`needs_review`；`details.engine` 为 `"bjj-23"` 或 `"mock"`；`details.isReliable` 为 `not needs_review`
  - `GET /api/treatments` 返回整份 catalog
  - `GET /api/treatments/<label>` 返回 `{ "label", "found", "item" }`
  - Vite：`/api/treatments` 与 `/api/analysis` 一样代理到 `http://127.0.0.1:5000`，且写在 `/api` 规则之前

- [ ] **Step 1: Write the failing tests**

Create `ml-bjj/tests/test_app_api.py`:

```python
from inference import PredictResult


class FakeClassifier:
    classes = ["健康", "小麦锈病", "稻瘟病"]

    def predict_detailed(self, image, crop_type="unknown"):
        if crop_type == "wheat":
            topk = [
                {"label": "小麦锈病", "confidence": 0.92},
                {"label": "健康", "confidence": 0.06},
                {"label": "稻瘟病", "confidence": 0.02},
            ]
            return PredictResult("小麦锈病", 0.92, topk, False)
        topk = [
            {"label": "稻瘟病", "confidence": 0.51},
            {"label": "水稻褐斑病", "confidence": 0.44},
            {"label": "健康", "confidence": 0.05},
        ]
        return PredictResult("稻瘟病", 0.51, topk, True)


def test_health_shape(monkeypatch):
    import app as serving_app

    monkeypatch.setattr(serving_app, "use_mock", lambda: True)
    client = serving_app.app.test_client()
    res = client.get("/health")
    assert res.status_code == 200
    body = res.get_json()
    assert body["status"] == "ok"
    assert "classes_count" in body
    assert "model_version" in body
    assert "engine" in body


def test_analyze_includes_topk_and_treatment(monkeypatch, tmp_path):
    import app as serving_app
    from io import BytesIO
    from PIL import Image

    monkeypatch.setattr(serving_app, "use_mock", lambda: False)
    monkeypatch.setattr(serving_app, "get_classifier", lambda: FakeClassifier())
    monkeypatch.setattr(serving_app, "load_model_meta", lambda: {
        "trained_at": "2026-08-20 00:00:00",
        "best_val_acc": 0.9,
        "classes": FakeClassifier.classes,
    })

    buf = BytesIO()
    Image.new("RGB", (8, 8), color=(10, 80, 10)).save(buf, format="JPEG")
    buf.seek(0)
    res = serving_app.app.test_client().post(
        "/api/analysis/image",
        data={"cropType": "wheat", "category": "pest", "file": (buf, "leaf.jpg", "image/jpeg")},
        content_type="multipart/form-data",
    )
    assert res.status_code == 200
    body = res.get_json()
    assert body["result"] == "小麦锈病"
    assert body["needs_review"] is False
    assert len(body["topk"]) == 3
    assert body["treatment"]["crop"] == "小麦"
    assert body["details"]["engine"] == "bjj-23"
    assert body["details"]["isReliable"] is True


def test_treatments_label_route():
    import app as serving_app

    res = serving_app.app.test_client().get("/api/treatments/稻瘟病")
    assert res.status_code == 200
    body = res.get_json()
    assert body["found"] is True
    assert body["item"]["crop"] == "水稻"
```

- [ ] **Step 2: Run test to verify it fails**

Run: `pytest ml-bjj/tests/test_app_api.py -v`

Expected: FAIL because `/health` lacks `classes_count` / `model_version`, and `/api/treatments/稻瘟病` is 404.

- [ ] **Step 3: Implement app.py changes**

Modify `ml-bjj/serving/app.py` (keep existing `validate_upload` / CORS / `CROP_LABELS`；`CROP_LABELS` 已含 `"rice": "水稻"`，不要删).

Replace the existing `from inference import get_classifier, resolve_weights_path` with:

```python
from datetime import datetime
import json
import torch

from crop_filter import CANONICAL_CLASSES, classes_for_crop
from knowledge import get_treatment_item, load_catalog
from predict_utils import needs_review, rank_topk
from inference import PredictResult, get_classifier, resolve_weights_path

META_PATH = Path(__file__).resolve().parents[1] / "models" / "pest-cls-meta.json"


def load_model_meta() -> dict:
    if META_PATH.is_file():
        return json.loads(META_PATH.read_text(encoding="utf-8"))
    return {"trained_at": None, "best_val_acc": None, "classes": []}


def model_version_payload(meta: dict | None = None) -> dict:
    meta = meta if meta is not None else load_model_meta()
    classes = meta.get("classes") or []
    return {
        "trained_at": meta.get("trained_at"),
        "best_val_acc": meta.get("best_val_acc"),
        "classes_count": len(classes),
    }


def engine_name() -> str:
    return "mock" if use_mock() else "bjj-23"
```

Replace `mock_predict` so it returns `PredictResult` and respects crop groups:

```python
def mock_predict(file, crop_type: str) -> PredictResult:
    raw = file.read()
    file.stream.seek(0)
    digest = sha256(raw).hexdigest()
    allowed = classes_for_crop(crop_type)
    labels = [c for c in CANONICAL_CLASSES if allowed is None or c in allowed]
    idx = int(digest[-4:], 16) % len(labels)
    conf = round(0.78 + (int(digest[:4], 16) % 100) / 500, 4)
    conf = min(conf, 0.98)
    probs = [0.0] * len(labels)
    probs[idx] = conf
    remain = max(0.0, 1.0 - conf)
    if len(labels) > 1:
        other = remain / (len(labels) - 1)
        for i in range(len(labels)):
            if i != idx:
                probs[i] = other
    topk = rank_topk(probs, labels, k=3)
    return PredictResult(
        label=labels[idx],
        confidence=float(conf),
        topk=topk,
        needs_review=needs_review(topk, float(conf)),
    )
```

Replace the predict branch in `analyze_image` with:

```python
        if use_mock():
            pred = mock_predict(file, crop_type)
        else:
            img = Image.open(file.stream)
            pred = get_classifier().predict_detailed(img, crop_type)

        level = classify_level(pred.label, pred.confidence)
        treatment, _found = get_treatment_item(pred.label)
        meta = load_model_meta()

        return jsonify(
            {
                "code": 200,
                "message": "success",
                "result": pred.label,
                "confidence": pred.confidence,
                "level": level,
                "topk": pred.topk,
                "needs_review": pred.needs_review,
                "model_version": model_version_payload(meta),
                "treatment": treatment,
                "details": {
                    "received_crop": crop_type,
                    "crop_label": CROP_LABELS.get(crop_type, "未知作物"),
                    "category": category,
                    "additionalInfo": additional_info,
                    "isReliable": not pred.needs_review,
                    "engine": engine_name(),
                    "weights": str(resolve_weights_path()) if not use_mock() else None,
                },
            }
        ), 200
```

Replace `health` with:

```python
@app.route("/health", methods=["GET"])
def health():
    meta = load_model_meta()
    classes = meta.get("classes") or []
    weights = resolve_weights_path()
    mtime = (
        datetime.fromtimestamp(weights.stat().st_mtime).isoformat()
        if weights.is_file()
        else None
    )
    return jsonify(
        {
            "status": "ok",
            "mock": use_mock(),
            "classes_count": len(classes),
            "classes": classes,
            "model_version": model_version_payload(meta),
            "weights_mtime": mtime,
            "cuda": bool(torch.cuda.is_available()),
            "engine": engine_name(),
        }
    ), 200
```

Add routes:

```python
@app.route("/api/treatments", methods=["GET"])
def treatments_all():
    return jsonify(load_catalog()), 200


@app.route("/api/treatments/<label>", methods=["GET"])
def treatments_one(label: str):
    item, found = get_treatment_item(label)
    return jsonify({"label": label, "found": found, "item": item}), 200
```

In `main()`, after loading classifier in non-mock mode, add:

```python
        meta_classes = load_model_meta().get("classes") or []
        if meta_classes and list(clf.classes) != list(meta_classes):
            raise SystemExit(
                f"权重 classes 与 pest-cls-meta.json 不一致: {len(clf.classes)} vs {len(meta_classes)}"
            )
        if len(clf.classes) != 23:
            raise SystemExit(f"期望 23 类，实际 {len(clf.classes)}: {clf.classes}")
```

In `vite.config.ts`, insert **before** the `/api` rule:

```ts
        '/api/treatments': {
          target: 'http://127.0.0.1:5000',
          changeOrigin: true
        },
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `pytest ml-bjj/tests/test_app_api.py -v`

Expected: PASS (3 passed)

- [ ] **Step 5: Commit**

```bash
git add ml-bjj/serving/app.py ml-bjj/tests/test_app_api.py vite.config.ts
git commit -m "feat(serving): enrich health and analysis responses; add treatments API"
```

---

### Task 7: 前端作物水稻 + 防治回退 + 预警等级

**Files:**
- Modify: `src/views/user/DataAnalysis.vue`
- Modify: `src/composables/useTreatmentGuide.ts`
- Test: 无现成前端单测；用 `pnpm exec vue-tsc --noEmit` 做类型门禁，并手工对照下列行为

**Interfaces:**
- Consumes: 分析响应的 `result`、`confidence`、`level`、`needs_review`（若缺省则仍用前端 `confidence < 0.7`）
- Produces: `cropLabels.rice === '水稻'`；下拉含水稻；`getTreatment` 未知标签返回 `summary` 含「暂无条目」的对象，不再返回 `catalog.items['健康']`；`createAlert.level` 使用接口 `level`（仅允许 `low|medium|high`，否则 `'medium'`）

- [ ] **Step 1: Confirm the current fallback**

`src/composables/useTreatmentGuide.ts` 中 `getTreatment` 现为 `return resolveByLabel(label) ?? catalog.items['健康']`。未知稻瘟等标签会显示健康文案，必须改掉。

- [ ] **Step 2: Implement composable and page changes**

In `src/composables/useTreatmentGuide.ts` add:

```ts
const MISSING_TREATMENT: TreatmentItem = {
  crop: '通用',
  crop_en: 'general',
  aliases: [],
  summary: '暂无该类防治条目，请以田间复核与当地植保意见为准。',
  risk_level: 'medium',
  symptoms: [],
  measures: {
    agronomic: ['保留清晰样本照片，送农技员复核后再用药。']
  },
  timing: '',
  safety: '在确认病名之前不要盲目施药。'
}

export function getTreatment(label: string): TreatmentItem {
  const found = resolveByLabel(label)
  if (found) return found
  return {
    ...MISSING_TREATMENT,
    summary: `暂无「${label.trim()}」的防治条目，请以田间复核与当地植保意见为准。`
  }
}
```

In `src/views/user/DataAnalysis.vue`:

1. Add option in the select (with wheat/corn/tomato):

```html
                      <a-select-option value="rice">水稻</a-select-option>
```

2. Extend `cropLabels`:

```ts
const cropLabels: Record<string, string> = {
  wheat: '小麦',
  corn: '玉米',
  tomato: '番茄',
  rice: '水稻'
}
```

3. In `handleConfirm`, after reading the response, use serving level and review flag:

```ts
    const aiResult = response.data.result as string
    const aiConfidence = response.data.confidence as number
    const rawLevel = response.data.level as string
    const level =
      rawLevel === 'low' || rawLevel === 'medium' || rawLevel === 'high' ? rawLevel : 'medium'
    const isHealthy = aiResult.includes('健康')
    const cropName = cropLabels[formState.cropType] ?? formState.cropType

    analysisResult.value = {
      result: aiResult,
      confidence: aiConfidence,
      isHealthy,
      cropType: formState.cropType,
      category: selectedCategory.value,
      analyzedAt: Date.now()
    }

    if (!isHealthy) {
      const defaultPointId = store.filteredMonitorPoints[0]?.id ?? store.monitorPoints[0]?.id ?? 1
      const pct = (aiConfidence <= 1 ? aiConfidence * 100 : aiConfidence).toFixed(1)
      await store.createAlert({
        pointId: defaultPointId,
        level,
        message: `[AI识别] 监测到 ${cropName} - ${aiResult} (置信度: ${pct}%)`,
        handled: false
      })
    }
```

Do not change `needsManualReview` and do not add `needs_review` onto `AnalysisResultView` in this task. Serving already returns `needs_review`; the page continues to use confidence < 70% for the review banner.

- [ ] **Step 3: Typecheck**

Run: `pnpm exec vue-tsc --noEmit`

Expected: exit code 0 (or only pre-existing errors unrelated to these files). If new errors are in the two edited files, fix them before committing.

- [ ] **Step 4: Commit**

```bash
git add src/views/user/DataAnalysis.vue src/composables/useTreatmentGuide.ts
git commit -m "feat(analysis): add rice crop and stop treatment fallback to healthy"
```

---

### Task 8: Mock 预警样例与 23 类中文病名对齐

**Files:**
- Modify: `src/mock/db.json`
- Test: `ml-bjj/tests/test_alert_copy.py`

**Interfaces:**
- Consumes: `crop_filter.CANONICAL_CLASSES`；`parseDiseaseFromAlert` 正则 `\[AI识别\].*?-\s*(.+?)\s*\(置信度`
- Produces: 所有 `message` 含 `[AI识别]` 的预警，解析出的病名 ∈ `CANONICAL_CLASSES`；作物中文名为 小麦/玉米/番茄/水稻 之一；删除桃/苹果病名

- [ ] **Step 1: Write the failing test**

Create `ml-bjj/tests/test_alert_copy.py`:

```python
import json
import re
from pathlib import Path

from crop_filter import CANONICAL_CLASSES

ROOT = Path(__file__).resolve().parents[2]
DB = ROOT / "src" / "mock" / "db.json"
AI_RE = re.compile(r"\[AI识别\].*?-\s*(.+?)\s*\(置信度")


def test_ai_alerts_use_canonical_labels():
    data = json.loads(DB.read_text(encoding="utf-8"))
    canonical = set(CANONICAL_CLASSES)
    bad = []
    for alert in data["alerts"]:
        message = alert.get("message", "")
        if "[AI识别]" not in message:
            continue
        match = AI_RE.search(message)
        if not match:
            bad.append(("unparsed", message))
            continue
        label = match.group(1).strip()
        if label not in canonical:
            bad.append((label, message))
    assert bad == [], bad
```

- [ ] **Step 2: Run test to verify it fails**

Run: `pytest ml-bjj/tests/test_alert_copy.py -v`

Expected: FAIL with tuples containing `桃缩叶病` / `苹果轮纹病` / `苹果腐烂病` / `桃疮痂病`.

- [ ] **Step 3: Replace those messages in `src/mock/db.json`**

Change only the `message` (and `level` where the new label is 健康 → `low`) for these ids:

| id | new message |
|----|-------------|
| 12 | `[AI识别] 监测到 水稻 - 稻瘟病 (置信度: 88.0%)` |
| 13 | `[AI识别] 监测到 水稻 - 稻颈瘟 (置信度: 96.0%)` |
| 14 | `[AI识别] 监测到 玉米 - 玉米南方锈病 (置信度: 94.0%)` |
| 18 | `[AI识别] 监测到 水稻 - 健康 (置信度: 98.0%)` ，`level` 改为 `"low"` |
| 19 | `[AI识别] 监测到 玉米 - 玉米小斑病 (置信度: 88.0%)` |
| 22 | `[AI识别] 监测到 玉米 - 健康 (置信度: 88.0%)` ，`level` 改为 `"low"` |
| 24 | `[AI识别] 监测到 水稻 - 水稻白叶枯病 (置信度: 98.0%)` |

Leave wheat 锈病/赤霉病 messages (ids 23, 25, 26, 27 等) unchanged if they already use canonical names.

Search the rest of `alerts` for `peach` / `apple` / `桃` / `苹果` inside `[AI识别]` lines and rewrite the same way if any remain.

- [ ] **Step 4: Run tests to verify they pass**

Run: `pytest ml-bjj/tests/test_alert_copy.py -v`

Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/mock/db.json ml-bjj/tests/test_alert_copy.py
git commit -m "fix(mock): align AI alert samples with 23-class Chinese labels"
```

---

### Task 9: 启动核对与全量回归

**Files:**
- Modify: none unless a test or startup check fails
- Test: `ml-bjj/tests/`

**Interfaces:**
- Consumes: 已训好的 `pest-cls-best.pt` 与 `pest-cls-meta.json`
- Produces: 全量 pytest 通过；`/health` 现场返回 23 类

- [ ] **Step 1: Unit suite**

Run:

```powershell
pytest ml-bjj/tests -v
```

Expected: all PASS.

- [ ] **Step 2: Meta gate**

Run:

```powershell
python -c "import json; from pathlib import Path; m=json.loads(Path('ml-bjj/models/pest-cls-meta.json').read_text(encoding='utf-8')); print(len(m['classes']), m['classes'])"
```

Expected: first number `23`，列表与 CANONICAL_CLASSES 集合相等。若不是 23：停止，把训好的 `pest-cls-best.pt` 与 `pest-cls-meta.json` 拷入 `ml-bjj/models/` 后再跑 Step 3。

- [ ] **Step 3: Boot serving and curl health**

```powershell
python ml-bjj\serving\app.py
```

In another terminal:

```powershell
curl http://127.0.0.1:5000/health
curl http://127.0.0.1:5000/api/treatments/稻瘟病
```

Expected: `classes_count` 为 23；稻瘟病 `found` 为 true。启动日志含 `23 类`。

- [ ] **Step 4: Manual analysis check**

1. 三终端：`pnpm dev`、`pnpm run mock`、`python ml-bjj\serving\app.py`
2. 智能分析选「水稻」，上传 `ml-bjj/data/bjj_cls/val/稻瘟病/` 下任一张（路径以仓库实际文件名为准）
3. 结果应为水稻类或健康；预警中心出现 `[AI识别] 监测到 水稻 - …`
4. 选「小麦」上传同一张水稻图，结果不得为稻瘟病（应为小麦类或健康）

- [ ] **Step 5: Commit only if you fixed bugs in this task**

If Step 1–4 passed with no extra diffs, skip commit. If you fixed a mismatch (for example meta vs CANONICAL_CLASSES order), commit that fix with a message that states the reason, such as:

```bash
git add -u
git commit -m "fix(serving): align startup class check with trained 23-class meta"
```

---

## Follow-up plans (not this document)

- P2：`analysisRecords`、feedback → `hard_cases`、识别统计接口
- P3：病名 × 气象 × 墒情规则链、batch、ONNX / gunicorn
