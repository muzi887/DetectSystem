# 本次未提交修改说明

> 生成时间：2026-05-28  
> 分支：`main`（与 `origin/main` 同步，**以下变更均未 commit / push**）  
> 统计：24 个文件修改、1 个文件删除、17+ 组新增未跟踪资源

---

## 一、变更概览

本次工作集中在 **前端 UI/UX 重构、玻璃拟态视觉统一、真实遥感素材接入、移动端响应式适配** 及少量配置/文档维护。核心目标：

1. 消除各业务页重复的玻璃卡片样式，统一设计令牌与组件
2. 修复遥感图中央遮挡、英文状态文案、空状态/加载态不一致等问题
3. 为 AppLayout 与各业务页补充平板/手机断点（≤992px、≤576px）
4. 接入 WebP 背景与 NDVI/墒情热力图真实素材，优化站点元信息与图标

---

## 二、新增文件（Untracked）

| 路径 | 说明 |
|------|------|
| `src/styles/glass-theme.css` | 全局 CSS 变量：颜色、玻璃效果、字体、断点 `--bp-tablet` / `--bp-mobile` |
| `src/styles/page-card.css` | 业务页公共样式：`.glass-page`、`.glass-panel`、`.glass-card-title`、`.glass-empty`、`.glass-loading-mask` 等 |
| `src/styles/leaflet-theme.css` | Leaflet 弹窗、聚合点、弹窗按钮全局样式（从地图/决策页抽出） |
| `src/components/GlassEmpty.vue` | 深色玻璃背景统一的空状态组件 |
| `src/utils/monitorStatus.ts` | 监测点状态中文映射（正常/预警/严重）与语义色 |
| `scripts/optimize-assets.mjs` | 大图 WebP 转换与遥感热力图压缩脚本（`npm run optimize-assets`） |
| `public/favicon.png` | 站点 favicon |
| `public/apple-touch-icon.png` | iOS 添加到主屏幕图标 |
| `src/assets/bg.webp` | 全站背景 WebP |
| `src/assets/wheat.webp` | 登录页作物图 WebP |
| `src/assets/ndvi-heatmap.jpg` / `.webp` | 无人机 NDVI 遥感热力图 |
| `src/assets/soil-moisture-heatmap.jpg` / `.webp` | GIS 土壤墒情热力图 |
| `src/assets/avatars/advisor-default.svg` | 关于页默认头像 |
| `src/assets/avatars/team-default.svg` | 关于页团队默认头像 |
| `src/assets/illustrations/agri-monitoring-line.svg` | 关于页插图 |
| `src/assets/ILLUSTRATION_SOURCES.md` | 插图/素材来源说明 |
| `deploy/线上故障排查笔记.md` | 502/404/反代/防火墙等线上故障对照与恢复流程 |

---

## 三、删除文件

| 路径 | 说明 |
|------|------|
| `src/style.css` | Vite 脚手架默认样式（`#app { max-width: 1280px }` 等），未在 `main.ts` 引入且与全屏布局冲突，已删除 |

---

## 四、已修改文件明细

### 4.1 入口与构建

| 文件 | 主要变更 |
|------|----------|
| `index.html` | `lang="zh-CN"`；系统 title/description/theme-color；favicon；Google Fonts（Noto Serif SC）预连接与预加载 |
| `src/main.ts` | 引入 `page-card.css`、`leaflet-theme.css`（原有 `glass-theme.css` 保留） |
| `package.json` | 新增脚本 `"optimize-assets": "node scripts/optimize-assets.mjs"` |
| `vite.config.ts` | 清理冗余注释，代理与 AutoImport 配置逻辑不变 |

### 4.2 布局与全局

| 文件 | 主要变更 |
|------|----------|
| `src/layouts/AppLayout.vue` | ① 导航项抽成 `navItems` 数组 ② ≤992px 汉堡按钮 + Ant Design Drawer 侧滑导航 ③ 窄屏标题缩短为「青禾智匠」④ ≤576px 隐藏搜索框与用户名 ⑤ 内容区内边距响应式 ⑥ 移除 scoped 内重复 `@import` 字体 |
| `src/composables/useGlobalSearch.ts` | 删除文件头注释，逻辑不变 |

### 4.3 业务页面

| 文件 | 主要变更 |
|------|----------|
| `src/views/user/Home.vue` | `dashboard-panel` → 公共 `glass-panel`；引入 `GlassEmpty`；首页 Grid 窄屏单列；指标卡片与快捷入口响应式；系统状态按待处理预警动态计算 |
| `src/views/user/RelatedData.vue` | 无人机/GIS 接入真实热力图（`<picture>` WebP 优先）；中央浮层改为左下角说明 + 右下角 NDVI/墒情色标图例；去掉 Tab 切换 500ms 假延迟；AI 结论区去掉 monospace；右侧 Tab 窄屏改底部网格；气象/头部/AI 区响应式 |
| `src/views/user/MapVisualization.vue` | 使用 `monitorStatus` 中文状态与统一颜色；玻璃卡片样式改用公共类；Leaflet 全局样式迁出；地图容器窄屏最小高度调整 |
| `src/views/user/DecisionSupport.vue` | 设备状态中文显示；嵌套卡片 `glass-widget-card`；左右分栏 ≤992px 改上下堆叠；监测数据网格 ≤576px 单列 |
| `src/views/user/WarningSystem.vue` | `glass-page` + `GlassEmpty`；预警列表窄屏标题换行；卡片内边距响应式 |
| `src/views/user/DataAnalysis.vue` | `glass-page` 公共卡片样式；分析区双栏 ≤992px 改单列；上传区窄屏缩小 |
| `src/views/user/About.vue` | 玻璃面板改用公共样式变量；移除重复 `@import` 字体；团队区接入 SVG 头像/插图（具体以页面为准） |
| `src/views/user/Login.vue` | 移除 scoped 内 Google Fonts `@import`，改用 `index.html` 全局加载 |

### 4.4 数据与后端（轻微）

| 文件 | 主要变更 |
|------|----------|
| `src/mock/db.json` | 部分历史 AI 预警标记为已处理；新增 3 条演示预警（手动触发、桃健康/桃疮痂病） |
| `src/mock/server.ts` | 删除注释 |
| `src/stores/data.ts` / `user.ts` | 删除注释 |
| `src/api/analysis.ts` | 删除注释 |
| `src/utils/http.ts` | 删除注释 |
| `server/app.py` | 删除模块/函数 docstring 注释 |
| `deploy/api_mock/server.js` | 删除文件头注释 |

### 4.5 文档

| 文件 | 主要变更 |
|------|----------|
| `README.md` | 文档索引增加 `deploy/线上故障排查笔记.md` 链接 |

---

## 五、功能与体验变更摘要（面向测试）

### 5.1 UI 统一

- 各页 Ant Card / 面板玻璃效果收敛至 `src/styles/page-card.css`
- 空状态统一为 `GlassEmpty`（浅色描述文字，适配深色背景）
- 加载遮罩统一为 `.glass-loading-mask`
- 标题衬线（Noto Serif SC）+ 正文无衬线（系统字体栈）

### 5.2 相关数据页

- 无人机 Tab：NDVI 植被指数真实热力图 + 5 级色标
- GIS Tab：土壤墒情热力图 + 5 级色标
- Tab 切换即时响应，无固定 500ms 等待

### 5.3 地图与决策

- 弹窗/详情中监测点状态显示「正常 / 预警 / 严重」，不再出现 `normal` / `warning` 等英文
- 颜色语义：绿 / 橙 / 红

### 5.4 响应式断点

| 断点 | 行为 |
|------|------|
| ≤992px | 顶栏汉堡 + 抽屉导航；首页单列；相关数据 Tab 移到底部 2 列；决策页上下布局 |
| ≤576px | 隐藏搜索与用户名；Tab 全宽；指标/气象/监测数据单列 |

---

## 六、本地验证建议

```bash
pnpm install
pnpm dev          # 5173
pnpm mock         # 3000
python server/app.py   # 5000
pnpm build        # 生产构建已通过（2026-05-28 验证）
```

浏览器 DevTools 设备模式建议宽度：**1280 / 992 / 576 / 375**。

---

## 七、建议 Git 提交范围

**Modified（24）：**

```
README.md
deploy/api_mock/server.js
index.html
package.json
server/app.py
src/api/analysis.ts
src/composables/useGlobalSearch.ts
src/layouts/AppLayout.vue
src/main.ts
src/mock/db.json
src/mock/server.ts
src/stores/data.ts
src/stores/user.ts
src/utils/http.ts
src/views/user/About.vue
src/views/user/DataAnalysis.vue
src/views/user/DecisionSupport.vue
src/views/user/Home.vue
src/views/user/Login.vue
src/views/user/MapVisualization.vue
src/views/user/RelatedData.vue
src/views/user/WarningSystem.vue
vite.config.ts
```

**Deleted（1）：** `src/style.css`

**Untracked（建议一并 add）：**  
`src/styles/`、`src/components/GlassEmpty.vue`、`src/utils/monitorStatus.ts`、`scripts/optimize-assets.mjs`、`public/*.png`、`src/assets/**`（含 webp/heatmap/avatars/illustrations）、`deploy/线上故障排查笔记.md`

---

## 八、建议 Git Commit Message

```
feat(ui): 统一玻璃拟态样式、接入遥感素材并完善移动端适配

- 抽取 glass-theme / page-card / leaflet-theme 公共样式，新增 GlassEmpty 与监测点状态中文映射
- 相关数据页接入 NDVI/墒情真实热力图与色标图例，移除 Tab 假延迟与中央遮挡浮层
- AppLayout 增加汉堡抽屉导航；首页、相关数据、智慧决策等页补充 ≤992px/≤576px 断点
- index.html 预加载 Noto Serif SC 与站点 favicon；删除未使用的 Vite 模板 style.css
- 新增 optimize-assets 脚本、WebP 背景/热力图资源及线上故障排查文档
```

---

*本文档随工作区未提交状态生成；commit 后请以 `git log` 与 PR 描述为准。*
